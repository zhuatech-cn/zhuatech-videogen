# Copyright 2026 上海如静知华信息科技有限公司 · https://www.zhuatech.cn/
services:
  api:
    build: ./backend
    environment:
      ZHUATECH_VIDEO_PROVIDER: ${ZHUATECH_VIDEO_PROVIDER:-local}
      ZHUATECH_VIDEO_BASE_URL: ${ZHUATECH_VIDEO_BASE_URL:-}
      ZHUATECH_VIDEO_MODEL: ${ZHUATECH_VIDEO_MODEL:-text-to-video-provider-model}
      ZHUATECH_VIDEO_API_KEY: ${ZHUATECH_VIDEO_API_KEY:-}
    ports: ["8080:8080"]
    healthcheck:
      test: ["CMD-SHELL", "if command -v nc >/dev/null 2>&1; then nc -z 127.0.0.1 8080; else bash -c '</dev/tcp/127.0.0.1/8080'; fi"]
      interval: 5s
      timeout: 3s
      retries: 40
      start_period: 15s
  web:
    image: nginx:1.27-alpine
    volumes: ["./frontend:/usr/share/nginx/html:ro", "./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro"]
    ports: ["8088:80"]
    depends_on:
      api:
        condition: service_healthy
